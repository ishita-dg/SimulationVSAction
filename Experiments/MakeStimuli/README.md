# Making experimental trials

The experimental trials are created using the [phystables Python package](https://github.com/kasmith/phystables), which is also used for the analysis of the data. It must be installed to run 'make_e1_trials.py' which creates the trials.
